# CORS
cors.allowedorigins=abc,def

# test keystore
test.keystore.enabled=true

profileconfig.jsonpath=../parameters

profileconfig.skip_dev_profiles=false

#Used by Time sensitive "VisibleSignature" unit tests -> The value is linked to the test pki (testpki project)
signing.time=1662649088181
